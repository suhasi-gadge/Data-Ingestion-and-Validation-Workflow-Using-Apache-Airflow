# Runtime dependencies for the DAG's Python callables.
# Apache Airflow itself is installed via the official Docker image (see
# docker-compose.yaml) so it is NOT pinned here — pinning it would conflict
# with the image's own pinned constraints.

pandas>=2.1,<3.0
requests>=2.31,<3.0
yfinance>=0.2.40,<0.3
# yfinance pulls in lxml + multitasking transitively; left implicit.

# --- development / test extras (optional) -----------------------------------
pytest>=8.0
